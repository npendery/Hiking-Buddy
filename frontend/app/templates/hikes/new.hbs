<div class="container text-center">
  {{#each validationErrors as |error|}}
  <div class="alert alert-danger" role="alert">
    {{error}}
  </div>
  {{/each}}
  <div class="well">
    <form {{action 'newHike' model on='submit'}}>
      <div>
        {{textarea class="form-control" placeholder="Name" value=name required=true}}
      </div>
      <div>
        {{textarea class="form-control" placeholder="Location" value=location required=true}}
      </div>
      <div>
        {{textarea class="form-control" placeholder="Description" value=description required=true}}
      </div>
      <div>
        {{textarea class="form-control" placeholder="Website" value=website}}
      </div>
      <div>
        <input type="Submit" class="button">
      </div>
    </form>
  </div>

  <a href="./hikes">Back to hikes</a>
</div>
